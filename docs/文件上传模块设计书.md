# 文件上传模块设计书

## 一、模块概述

### 1.1 模块定位

文件上传模块（FileStorageService）是Novel2Script系统的入口模块，负责接收用户上传的小说文件，进行格式验证和安全检查，将文件存储到服务器，并记录文件元数据到数据库，为后续的文本解析和转换提供基础数据。

### 1.2 设计目标

| 目标 | 说明 |
|------|------|
| 格式兼容 | 支持txt、md、docx三种文档格式上传 |
| 安全可靠 | 文件类型验证、大小限制、防恶意上传 |
| 存储稳定 | UUID重命名、目录结构清晰、路径可配置 |
| 元数据完整 | 记录文件名、大小、类型、上传时间等信息 |
| 易于扩展 | 支持后续扩展更多文件格式 |

### 1.3 依赖关系

```
┌─────────────────────┐
│      前端页面        │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   NovelController   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  FileStorageService │◄──── 本模块
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐  ┌─────────────────────┐
│   文件存储系统       │  │   MySQL数据库        │
│  (本地磁盘)          │  │  (novel_upload表)    │
└─────────────────────┘  └─────────────────────┘
```

---

## 二、功能需求

### 2.1 功能列表

| 编号 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| F-001 | 文件上传 | P0 | 接收multipart/form-data文件上传 |
| F-002 | 格式验证 | P0 | 验证文件扩展名和MIME类型 |
| F-003 | 大小限制 | P0 | 限制文件大小不超过10MB |
| F-004 | 文件存储 | P0 | UUID重命名存储到本地磁盘 |
| F-005 | 元数据保存 | P0 | 保存文件信息到数据库 |
| F-006 | 文件删除 | P1 | 支持删除已上传的文件 |
| F-007 | 文件查询 | P1 | 根据ID查询文件信息 |

### 2.2 输入输出

**输入：**
- MultipartFile：上传的文件对象

**输出：**
- NovelUploadVO对象，包含：
  - id：文件ID
  - originalFilename：原始文件名
  - storedFilename：存储文件名
  - fileType：文件类型
  - fileSize：文件大小
  - status：状态
  - uploadTime：上传时间

### 2.3 业务规则

| 规则编号 | 规则描述 |
|----------|----------|
| BR-001 | 文件大小不得超过10MB |
| BR-002 | 仅支持txt、md、docx三种格式 |
| BR-003 | 文件名不能为空 |
| BR-004 | 文件内容不能为空 |
| BR-005 | 使用UUID重命名防止文件名冲突 |
| BR-006 | 按日期分目录存储便于管理 |
| BR-007 | 上传成功后状态为UPLOADED |

---

## 三、接口设计

### 3.1 RESTful API

| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| POST | /api/novel/upload | 上传小说文件 | multipart/form-data | NovelUploadVO |
| GET | /api/novel/{id} | 获取小说信息 | - | NovelUploadVO |
| GET | /api/novels | 获取小说列表 | page, size | PageResult |
| DELETE | /api/novel/{id} | 删除小说 | - | Result |

### 3.2 服务接口

```java
public interface FileStorageService {
    
    /**
     * 存储文件
     * 
     * @param file 上传的文件
     * @return 文件上传实体
     * @throws FileStorageException 存储异常
     */
    NovelUpload store(MultipartFile file) throws FileStorageException;
    
    /**
     * 删除文件
     * 
     * @param fileId 文件ID
     * @throws FileStorageException 存储异常
     */
    void delete(Long fileId) throws FileStorageException;
    
    /**
     * 获取文件信息
     * 
     * @param fileId 文件ID
     * @return 文件上传实体
     */
    NovelUpload getFile(Long fileId);
    
    /**
     * 获取文件列表
     * 
     * @param pageable 分页参数
     * @return 文件列表
     */
    Page<NovelUpload> listFiles(Pageable pageable);
    
    /**
     * 获取文件路径
     * 
     * @param fileId 文件ID
     * @return 文件路径
     */
    Path getFilePath(Long fileId);
}
```

### 3.3 数据传输对象

#### NovelUploadVO（响应对象）

```java
@Data
@Builder
public class NovelUploadVO {
    
    /**
     * 文件ID
     */
    private Long id;
    
    /**
     * 原始文件名
     */
    private String originalFilename;
    
    /**
     * 存储文件名
     */
    private String storedFilename;
    
    /**
     * 文件类型
     */
    private String fileType;
    
    /**
     * 文件大小（字节）
     */
    private Long fileSize;
    
    /**
     * 文件大小（可读格式）
     */
    private String fileSizeFormatted;
    
    /**
     * 状态
     */
    private String status;
    
    /**
     * 上传时间
     */
    private LocalDateTime uploadTime;
}
```

#### Result（通用响应）

```java
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class Result<T> {
    
    /**
     * 状态码
     */
    private int code;
    
    /**
     * 消息
     */
    private String message;
    
    /**
     * 数据
     */
    private T data;
    
    public static <T> Result<T> success(T data) {
        return Result.<T>builder()
            .code(200)
            .message("success")
            .data(data)
            .build();
    }
    
    public static <T> Result<T> error(int code, String message) {
        return Result.<T>builder()
            .code(code)
            .message(message)
            .build();
    }
}
```

#### PageResult（分页响应）

```java
@Data
@Builder
public class PageResult<T> {
    
    /**
     * 数据列表
     */
    private List<T> content;
    
    /**
     * 总记录数
     */
    private long totalElements;
    
    /**
     * 总页数
     */
    private int totalPages;
    
    /**
     * 当前页码
     */
    private int pageNumber;
    
    /**
     * 每页大小
     */
    private int pageSize;
}
```

---

## 四、数据库设计

### 4.1 设计原则

1. **只存储文件路径**：不存储文件内容，需要时读取文件路径再获取内容
2. **减少数据库压力**：避免大文本字段影响查询性能
3. **文件系统更适合**：大文件存储在文件系统中，数据库只存元数据

### 4.2 实体类

```java
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@TableName("novel_upload")
public class NovelUpload {
    
    @TableId(type = IdType.AUTO)
    private Long id;
    
    @TableField("original_filename")
    private String originalFilename;
    
    @TableField("stored_filename")
    private String storedFilename;
    
    @TableField("file_path")
    private String filePath;
    
    @TableField("file_size")
    private Long fileSize;
    
    @TableField("file_type")
    private String fileType;
    
    @TableField("chapter_count")
    private Integer chapterCount;
    
    @TableField("status")
    private NovelStatus status;
    
    @TableField("upload_time")
    private LocalDateTime uploadTime;
    
    @TableField("update_time")
    private LocalDateTime updateTime;
    
    @TableField(fill = FieldFill.INSERT)
    public void setUploadTime() {
        this.uploadTime = LocalDateTime.now();
    }
    
    @TableField(fill = FieldFill.INSERT_UPDATE)
    public void setUpdateTime() {
        this.updateTime = LocalDateTime.now();
    }
}
```

### 4.3 枚举定义

```java
public enum NovelStatus {
    
    UPLOADED("已上传"),
    PARSING("解析中"),
    PARSED("解析完成"),
    ERROR("解析失败");
    
    private final String description;
    
    NovelStatus(String description) {
        this.description = description;
    }
    
    public String getDescription() {
        return description;
    }
}
```

### 4.4 Mapper接口

```java
@Mapper
public interface NovelUploadMapper extends BaseMapper<NovelUpload> {
    
    /**
     * 根据状态查询
     */
    List<NovelUpload> selectByStatus(@Param("status") NovelStatus status);
    
    /**
     * 根据文件类型查询
     */
    List<NovelUpload> selectByFileType(@Param("fileType") String fileType);
}
```

#### NovelUploadMapper.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiniu.novel2script.mapper.NovelUploadMapper">
    
    <select id="selectByStatus" resultType="com.qiniu.novel2script.entity.NovelUpload">
        SELECT * FROM novel_upload WHERE status = #{status} ORDER BY upload_time DESC
    </select>
    
    <select id="selectByFileType" resultType="com.qiniu.novel2script.entity.NovelUpload">
        SELECT * FROM novel_upload WHERE file_type = #{fileType} ORDER BY upload_time DESC
    </select>
    
</mapper>
```

### 4.5 表结构

```sql
CREATE TABLE novel_upload (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
    original_filename VARCHAR(255) NOT NULL COMMENT '原始文件名',
    stored_filename VARCHAR(255) NOT NULL COMMENT '存储文件名（UUID）',
    file_path VARCHAR(500) NOT NULL COMMENT '文件存储路径',
    file_size BIGINT NOT NULL COMMENT '文件大小（字节）',
    file_type VARCHAR(20) NOT NULL COMMENT '文件类型（txt/md/docx）',
    chapter_count INT DEFAULT 0 COMMENT '识别到的章节数',
    status VARCHAR(20) NOT NULL COMMENT '状态',
    upload_time DATETIME NOT NULL COMMENT '上传时间',
    update_time DATETIME NOT NULL COMMENT '更新时间',
    INDEX idx_status (status),
    INDEX idx_file_type (file_type),
    INDEX idx_upload_time (upload_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='小说上传记录表';
```

---

## 五、类设计

### 5.1 类结构

```
com.qiniu.novel2script
├── controller/
│   └── NovelController.java              # 小说API控制器
├── service/
│   ├── FileStorageService.java           # 文件存储服务接口
│   └── impl/
│       └── FileStorageServiceImpl.java   # 文件存储服务实现
├── entity/
│   └── NovelUpload.java                  # 小说上传实体
├── vo/
│   ├── NovelUploadVO.java                # 小说上传VO
│   ├── Result.java                       # 通用响应
│   └── PageResult.java                   # 分页响应
├── mapper/
│   └── NovelUploadMapper.java            # 小说Mapper接口
├── enums/
│   └── NovelStatus.java                  # 小说状态枚举
├── exception/
│   └── FileStorageException.java         # 文件存储异常
└── config/
    └── StorageProperties.java            # 存储配置
```

### 5.2 核心类实现

#### NovelController

```java
@RestController
@RequestMapping("/api")
@Slf4j
@RequiredArgsConstructor
public class NovelController {
    
    private final FileStorageService fileStorageService;
    
    /**
     * 上传小说文件
     */
    @PostMapping("/novel/upload")
    public Result<NovelUploadVO> uploadNovel(@RequestParam("file") MultipartFile file) {
        log.info("收到文件上传请求: {}", file.getOriginalFilename());
        
        // 1. 存储文件
        NovelUpload novelUpload = fileStorageService.store(file);
        
        // 2. 转换为VO
        NovelUploadVO vo = convertToVO(novelUpload);
        
        return Result.success(vo);
    }
    
    /**
     * 获取小说信息
     */
    @GetMapping("/novel/{id}")
    public Result<NovelUploadVO> getNovel(@PathVariable Long id) {
        NovelUpload novelUpload = fileStorageService.getFile(id);
        NovelUploadVO vo = convertToVO(novelUpload);
        return Result.success(vo);
    }
    
    /**
     * 获取小说列表
     */
    @GetMapping("/novels")
    public Result<PageResult<NovelUploadVO>> listNovels(
            @RequestParam(defaultValue = "0") int page,
            @RequestParam(defaultValue = "10") int size) {
        
        Pageable pageable = PageRequest.of(page, size, Sort.by("uploadTime").descending());
        Page<NovelUpload> novelPage = fileStorageService.listFiles(pageable);
        
        // 转换为VO列表
        List<NovelUploadVO> voList = novelPage.getContent().stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());
        
        // 构建分页结果
        PageResult<NovelUploadVO> pageResult = PageResult.<NovelUploadVO>builder()
            .content(voList)
            .totalElements(novelPage.getTotalElements())
            .totalPages(novelPage.getTotalPages())
            .pageNumber(novelPage.getNumber())
            .pageSize(novelPage.getSize())
            .build();
        
        return Result.success(pageResult);
    }
    
    /**
     * 删除小说
     */
    @DeleteMapping("/novel/{id}")
    public Result<Void> deleteNovel(@PathVariable Long id) {
        fileStorageService.delete(id);
        return Result.success(null);
    }
    
    private NovelUploadVO convertToVO(NovelUpload entity) {
        return NovelUploadVO.builder()
            .id(entity.getId())
            .originalFilename(entity.getOriginalFilename())
            .storedFilename(entity.getStoredFilename())
            .fileType(entity.getFileType())
            .fileSize(entity.getFileSize())
            .fileSizeFormatted(formatFileSize(entity.getFileSize()))
            .status(entity.getStatus().getDescription())
            .uploadTime(entity.getUploadTime())
            .build();
    }
    
    private String formatFileSize(long size) {
        if (size < 1024) {
            return size + " B";
        } else if (size < 1024 * 1024) {
            return String.format("%.2f KB", size / 1024.0);
        } else {
            return String.format("%.2f MB", size / (1024.0 * 1024));
        }
    }
}
```

#### FileStorageServiceImpl

```java
@Service
@Slf4j
@RequiredArgsConstructor
public class FileStorageServiceImpl implements FileStorageService {
    
    private final NovelUploadMapper novelUploadMapper;
    private final StorageProperties storageProperties;
    
    /**
     * 支持的文件类型
     */
    private static final Set<String> SUPPORTED_TYPES = Set.of("txt", "md", "docx");
    
    /**
     * 最大文件大小（10MB）
     */
    private static final long MAX_FILE_SIZE = 10 * 1024 * 1024;
    
    @Override
    public NovelUpload store(MultipartFile file) throws FileStorageException {
        // 1. 验证文件
        validateFile(file);
        
        // 2. 获取文件信息
        String originalFilename = file.getOriginalFilename();
        String extension = getFileExtension(originalFilename);
        long fileSize = file.getSize();
        
        // 3. 生成存储文件名
        String storedFilename = generateStoredFilename(extension);
        
        // 4. 生成存储路径
        String relativePath = generateRelativePath(storedFilename);
        Path absolutePath = storageProperties.getBasePath().resolve(relativePath);
        
        try {
            // 5. 创建目录
            Files.createDirectories(absolutePath.getParent());
            
            // 6. 存储文件
            Files.copy(file.getInputStream(), absolutePath, StandardCopyOption.REPLACE_EXISTING);
            log.info("文件存储成功: {}", absolutePath);
            
            // 7. 保存元数据
            NovelUpload novelUpload = NovelUpload.builder()
                .originalFilename(originalFilename)
                .storedFilename(storedFilename)
                .filePath(relativePath)
                .fileSize(fileSize)
                .fileType(extension)
                .chapterCount(0)
                .status(NovelStatus.UPLOADED)
                .uploadTime(LocalDateTime.now())
                .updateTime(LocalDateTime.now())
                .build();
            
            novelUploadMapper.insert(novelUpload);
            log.info("文件元数据保存成功: id={}", novelUpload.getId());
            
            return novelUpload;
            
        } catch (IOException e) {
            log.error("文件存储失败: {}", e.getMessage(), e);
            throw new FileStorageException("文件存储失败: " + e.getMessage(), e);
        }
    }
    
    @Override
    public void delete(Long fileId) throws FileStorageException {
        // 1. 查询文件信息
        NovelUpload novelUpload = novelUploadMapper.selectById(fileId);
        if (novelUpload == null) {
            throw new FileStorageException("文件不存在: " + fileId);
        }
        
        try {
            // 2. 删除物理文件
            Path filePath = storageProperties.getBasePath().resolve(novelUpload.getFilePath());
            Files.deleteIfExists(filePath);
            log.info("物理文件删除成功: {}", filePath);
            
            // 3. 删除数据库记录
            novelUploadMapper.deleteById(fileId);
            log.info("数据库记录删除成功: id={}", fileId);
            
        } catch (IOException e) {
            log.error("文件删除失败: {}", e.getMessage(), e);
            throw new FileStorageException("文件删除失败: " + e.getMessage(), e);
        }
    }
    
    @Override
    public NovelUpload getFile(Long fileId) {
        NovelUpload novelUpload = novelUploadMapper.selectById(fileId);
        if (novelUpload == null) {
            throw new FileStorageException("文件不存在: " + fileId);
        }
        return novelUpload;
    }
    
    @Override
    public Page<NovelUpload> listFiles(Pageable pageable) {
        // 使用MyBatis Plus分页
        Page<NovelUpload> page = new Page<>(pageable.getPageNumber(), pageable.getPageSize());
        QueryWrapper<NovelUpload> queryWrapper = new QueryWrapper<>();
        queryWrapper.orderByDesc("upload_time");
        return novelUploadMapper.selectPage(page, queryWrapper);
    }
    
    @Override
    public Path getFilePath(Long fileId) {
        NovelUpload novelUpload = getFile(fileId);
        return storageProperties.getBasePath().resolve(novelUpload.getFilePath());
    }
    
    /**
     * 验证文件
     */
    private void validateFile(MultipartFile file) {
        // 检查文件是否为空
        if (file.isEmpty()) {
            throw new FileStorageException("文件不能为空");
        }
        
        // 检查文件名
        String originalFilename = file.getOriginalFilename();
        if (originalFilename == null || originalFilename.isBlank()) {
            throw new FileStorageException("文件名不能为空");
        }
        
        // 检查文件类型
        String extension = getFileExtension(originalFilename);
        if (!SUPPORTED_TYPES.contains(extension.toLowerCase())) {
            throw new FileStorageException("不支持的文件类型: " + extension + "，仅支持: " + SUPPORTED_TYPES);
        }
        
        // 检查文件大小
        if (file.getSize() > MAX_FILE_SIZE) {
            throw new FileStorageException("文件大小超过限制: " + formatFileSize(file.getSize()) + "，最大允许: 10MB");
        }
    }
    
    /**
     * 获取文件扩展名
     */
    private String getFileExtension(String filename) {
        if (filename == null) {
            return "";
        }
        int lastDotIndex = filename.lastIndexOf('.');
        if (lastDotIndex == -1) {
            return "";
        }
        return filename.substring(lastDotIndex + 1).toLowerCase();
    }
    
    /**
     * 生成存储文件名
     */
    private String generateStoredFilename(String extension) {
        return UUID.randomUUID().toString() + "." + extension;
    }
    
    /**
     * 生成相对路径（按日期分目录）
     */
    private String generateRelativePath(String storedFilename) {
        String datePath = LocalDate.now().format(DateTimeFormatter.ofPattern("yyyy/MM/dd"));
        return datePath + "/" + storedFilename;
    }
    
    /**
     * 格式化文件大小
     */
    private String formatFileSize(long size) {
        if (size < 1024) {
            return size + " B";
        } else if (size < 1024 * 1024) {
            return String.format("%.2f KB", size / 1024.0);
        } else {
            return String.format("%.2f MB", size / (1024.0 * 1024));
        }
    }
}
```

#### StorageProperties

```java
@Data
@Component
@ConfigurationProperties(prefix = "storage")
public class StorageProperties {
    
    /**
     * 基础存储路径
     */
    private Path basePath = Paths.get("./uploads");
    
    /**
     * 小说文件存储路径
     */
    private Path novelPath;
    
    /**
     * 剧本文件存储路径
     */
    private Path scriptPath;
    
    @PostConstruct
    public void init() {
        if (novelPath == null) {
            novelPath = basePath.resolve("novels");
        }
        if (scriptPath == null) {
            scriptPath = basePath.resolve("scripts");
        }
    }
}
```

#### FileStorageException

```java
public class FileStorageException extends RuntimeException {
    
    public FileStorageException(String message) {
        super(message);
    }
    
    public FileStorageException(String message, Throwable cause) {
        super(message, cause);
    }
}
```

---

## 六、算法设计

### 6.1 文件存储路径算法

```
输入：原始文件名 originalFilename
输出：相对存储路径 relativePath

1. 提取文件扩展名 extension
   - 从文件名中获取最后一个"."后的内容
   - 转换为小写

2. 生成UUID文件名 storedFilename
   - storedFilename = UUID.randomUUID() + "." + extension

3. 生成日期目录 datePath
   - 获取当前日期
   - 格式化为 "yyyy/MM/dd"

4. 拼接相对路径
   - relativePath = datePath + "/" + storedFilename

返回 relativePath
```

### 6.2 文件验证算法

```
输入：MultipartFile file
输出：验证结果（通过/抛出异常）

1. 检查文件是否为空
   - 如果 file.isEmpty() 为 true，抛出异常

2. 检查文件名
   - 如果 originalFilename 为空或空白，抛出异常

3. 检查文件类型
   - 提取扩展名
   - 如果不在支持列表中，抛出异常

4. 检查文件大小
   - 如果 file.getSize() > 10MB，抛出异常

返回 通过
```

---

## 七、配置项

### 7.1 application.yml

```yaml
server:
  port: 8080

spring:
  application:
    name: novel-to-script
  
  datasource:
    url: jdbc:mysql://localhost:3306/novel2script?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
  type-aliases-package: com.qiniu.novel2script.entity
  mapper-locations: classpath:mapper/*.xml

# 文件存储配置
storage:
  base-path: ./uploads
  novel-path: ${storage.base-path}/novels
  script-path: ${storage.base-path}/scripts

# 日志配置
logging:
  level:
    com.qiniu.novel2script: DEBUG
    org.springframework.web: INFO
```

---

## 八、测试用例

### 8.1 单元测试

```java
@SpringBootTest
@Slf4j
class FileStorageServiceTest {
    
    @Autowired
    private FileStorageService fileStorageService;
    
    @Autowired
    private NovelUploadMapper novelUploadMapper;
    
    @Test
    void testStoreTxtFile() throws IOException {
        // 准备
        Path tempFile = Files.createTempFile("test", ".txt");
        Files.writeString(tempFile, "测试内容");
        MockMultipartFile file = new MockMultipartFile(
            "file",
            "test.txt",
            "text/plain",
            Files.readAllBytes(tempFile)
        );
        
        // 执行
        NovelUpload result = fileStorageService.store(file);
        
        // 验证
        assertNotNull(result);
        assertNotNull(result.getId());
        assertEquals("test.txt", result.getOriginalFilename());
        assertEquals("txt", result.getFileType());
        assertEquals(NovelStatus.UPLOADED, result.getStatus());
        
        // 清理
        fileStorageService.delete(result.getId());
        Files.deleteIfExists(tempFile);
    }
    
    @Test
    void testStoreMdFile() throws IOException {
        // 准备
        Path tempFile = Files.createTempFile("test", ".md");
        Files.writeString(tempFile, "# 标题\n\n测试内容");
        MockMultipartFile file = new MockMultipartFile(
            "file",
            "test.md",
            "text/markdown",
            Files.readAllBytes(tempFile)
        );
        
        // 执行
        NovelUpload result = fileStorageService.store(file);
        
        // 验证
        assertNotNull(result);
        assertEquals("md", result.getFileType());
        
        // 清理
        fileStorageService.delete(result.getId());
        Files.deleteIfExists(tempFile);
    }
    
    @Test
    void testStoreEmptyFile() {
        // 准备
        MockMultipartFile file = new MockMultipartFile(
            "file",
            "empty.txt",
            "text/plain",
            new byte[0]
        );
        
        // 执行和验证
        assertThrows(FileStorageException.class, () -> {
            fileStorageService.store(file);
        });
    }
    
    @Test
    void testStoreUnsupportedFileType() throws IOException {
        // 准备
        Path tempFile = Files.createTempFile("test", ".pdf");
        Files.writeString(tempFile, "测试内容");
        MockMultipartFile file = new MockMultipartFile(
            "file",
            "test.pdf",
            "application/pdf",
            Files.readAllBytes(tempFile)
        );
        
        // 执行和验证
        assertThrows(FileStorageException.class, () -> {
            fileStorageService.store(file);
        });
        
        // 清理
        Files.deleteIfExists(tempFile);
    }
    
    @Test
    void testDeleteFile() {
        // 准备 - 先上传一个文件
        Path tempFile = Files.createTempFile("test", ".txt");
        MockMultipartFile file = new MockMultipartFile(
            "file",
            "test.txt",
            "text/plain",
            new byte[]{1, 2, 3}
        );
        NovelUpload uploaded = fileStorageService.store(file);
        
        // 执行
        fileStorageService.delete(uploaded.getId());
        
        // 验证
        assertNull(novelUploadMapper.selectById(uploaded.getId()));
    }
    
    @Test
    void testGetFile() {
        // 准备
        Path tempFile = Files.createTempFile("test", ".txt");
        MockMultipartFile file = new MockMultipartFile(
            "file",
            "test.txt",
            "text/plain",
            new byte[]{1, 2, 3}
        );
        NovelUpload uploaded = fileStorageService.store(file);
        
        // 执行
        NovelUpload result = fileStorageService.getFile(uploaded.getId());
        
        // 验证
        assertNotNull(result);
        assertEquals(uploaded.getId(), result.getId());
        
        // 清理
        fileStorageService.delete(uploaded.getId());
    }
    
    @Test
    void testGetFileNotFound() {
        // 执行和验证
        assertThrows(FileStorageException.class, () -> {
            fileStorageService.getFile(999L);
        });
    }
    
    @Test
    void testListFiles() {
        // 准备 - 上传多个文件
        for (int i = 0; i < 5; i++) {
            MockMultipartFile file = new MockMultipartFile(
                "file",
                "test" + i + ".txt",
                "text/plain",
                new byte[]{1, 2, 3}
            );
            fileStorageService.store(file);
        }
        
        // 执行
        Pageable pageable = PageRequest.of(0, 10);
        Page<NovelUpload> result = fileStorageService.listFiles(pageable);
        
        // 验证
        assertNotNull(result);
        assertTrue(result.getRecords().size() >= 5);
    }
}
```

### 8.2 控制器测试

```java
@WebMvcTest(NovelController.class)
@Slf4j
class NovelControllerTest {
    
    @Autowired
    private MockMvc mockMvc;
    
    @MockBean
    private FileStorageService fileStorageService;
    
    @Test
    void testUploadNovel() throws Exception {
        // 准备
        MockMultipartFile file = new MockMultipartFile(
            "file",
            "test.txt",
            "text/plain",
            "测试内容".getBytes()
        );
        
        NovelUpload mockResult = NovelUpload.builder()
            .id(1L)
            .originalFilename("test.txt")
            .storedFilename("uuid.txt")
            .fileType("txt")
            .fileSize(12L)
            .status(NovelStatus.UPLOADED)
            .uploadTime(LocalDateTime.now())
            .build();
        
        when(fileStorageService.store(any())).thenReturn(mockResult);
        
        // 执行和验证
        mockMvc.perform(multipart("/api/novel/upload").file(file))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(200))
            .andExpect(jsonPath("$.data.originalFilename").value("test.txt"))
            .andExpect(jsonPath("$.data.fileType").value("txt"));
    }
    
    @Test
    void testGetNovel() throws Exception {
        // 准备
        NovelUpload mockResult = NovelUpload.builder()
            .id(1L)
            .originalFilename("test.txt")
            .storedFilename("uuid.txt")
            .fileType("txt")
            .fileSize(12L)
            .status(NovelStatus.UPLOADED)
            .uploadTime(LocalDateTime.now())
            .build();
        
        when(fileStorageService.getFile(1L)).thenReturn(mockResult);
        
        // 执行和验证
        mockMvc.perform(get("/api/novel/1"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(200))
            .andExpect(jsonPath("$.data.id").value(1));
    }
    
    @Test
    void testDeleteNovel() throws Exception {
        // 执行和验证
        mockMvc.perform(delete("/api/novel/1"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(200));
        
        verify(fileStorageService).delete(1L);
    }
}
```

### 8.3 测试数据

| 测试场景 | 输入 | 预期结果 |
|----------|------|----------|
| 上传TXT文件 | test.txt（100字节） | 上传成功，返回文件信息 |
| 上传MD文件 | test.md（500字节） | 上传成功，返回文件信息 |
| 上传DOCX文件 | test.docx（1MB） | 上传成功，返回文件信息 |
| 上传空文件 | 空文件 | 抛出异常 |
| 上传超大文件 | 11MB文件 | 抛出异常 |
| 上传不支持格式 | test.pdf | 抛出异常 |
| 删除文件 | 已存在的文件ID | 删除成功 |
| 删除不存在文件 | 不存在的ID | 抛出异常 |

---

## 九、性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 上传响应时间 | <500ms | 包含文件存储和数据库写入 |
| 并发上传支持 | 10个/秒 | 同时处理的上传请求数 |
| 文件存储成功率 | >99.9% | 成功存储的比例 |
| 数据库写入时间 | <50ms | 元数据保存耗时 |

---

## 十、安全考虑

| 安全项 | 措施 |
|--------|------|
| 文件类型验证 | 验证扩展名和MIME类型 |
| 文件大小限制 | 限制最大10MB |
| 文件名安全 | UUID重命名，防止路径遍历 |
| 存储隔离 | 按日期分目录，避免文件冲突 |
| 异常处理 | 友好错误提示，不暴露系统信息 |

---

## 十一、后续优化

| 优化项 | 优先级 | 说明 |
|--------|--------|------|
| 文件去重 | P1 | 基于MD5判断重复文件 |
| 断点续传 | P2 | 支持大文件断点续传 |
| 批量上传 | P2 | 支持多文件同时上传 |
| 上传进度 | P2 | 实时显示上传进度 |
| 云存储支持 | P3 | 支持阿里云OSS、七牛云等 |

---

**文档编写：** 张恒嘉  
**最后更新：** 2026-06-06  
**文档状态：** 已审核