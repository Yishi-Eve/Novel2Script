# 章节分割模块设计书

## 一、模块概述

### 1.1 模块定位

章节分割模块（ChapterSplitterService）是Novel2Script系统的核心处理模块，负责将文本解析模块输出的纯文本内容按照章节结构进行智能识别和分割，为后续的AI转换提供结构化的章节数据。该模块是连接文本解析与AI转换的桥梁，直接影响最终剧本的质量。

### 1.2 设计目标

| 目标 | 说明 |
|------|------|
| 格式兼容 | 支持多种常见章节标记格式（第X章、Chapter X、卷X篇X等） |
| 识别准确 | 基于正则表达式和启发式规则，准确识别章节边界 |
| 结果持久化 | 将分割结果保存为JSON文件，便于后续查询和调试 |
| 章节验证 | 确保识别到的章节数不少于3个（项目要求） |
| 性能高效 | 大文件处理流畅，内存占用合理 |

### 1.3 依赖关系

```
┌─────────────────────┐
│    文本解析模块      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│    章节分割模块      │◄──── 本模块
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│    AI转换模块        │
└─────────────────────┘
```

---

## 二、处理流程

### 2.1 整体流程

```
文本解析模块输出 cleanText
           │
           ▼
┌─────────────────────────────────────┐
│  章节分割模块（ChapterSplitterService）│
│  ① 文本预处理（去除广告、整理格式）    │
│  ② 正则匹配章节标题                   │
│  ③ 过滤误匹配（目录、注释等）         │
│  ④ 分割章节内容                       │
│  ⑤ 验证章节数量（≥3）                 │
│  ⑥ 生成章节元数据                     │
│  ⑦ 保存结果到JSON文件                 │
└─────────────────────────────────────┘
           │
           │ 传递：章节列表 + 文件路径
           ▼
┌─────────────────────────────────────┐
│  AI转换模块                          │
│  - 逐章读取内容                       │
│  - 调用百炼API转换                    │
└─────────────────────────────────────┘
```

### 2.2 分割流程详解

**步骤1：文本预处理**
- 输入：cleanText（文本解析模块输出）
- 逻辑：去除广告文本、整理格式、统一标点
- 输出：预处理后的文本

**步骤2：正则匹配章节标题**
- 输入：预处理后的文本
- 逻辑：使用预定义的正则表达式模式匹配章节标题
- 输出：候选章节标题列表（包含位置信息）

**步骤3：过滤误匹配**
- 输入：候选章节标题列表
- 逻辑：过滤目录、注释、引文中的误匹配
- 输出：有效章节标题列表

**步骤4：分割章节内容**
- 输入：有效章节标题列表 + 原始文本
- 逻辑：根据标题位置分割文本
- 输出：章节内容列表

**步骤5：验证章节数量**
- 输入：章节内容列表
- 逻辑：检查章节数是否≥3
- 输出：验证结果（不满足则抛出异常）

**步骤6：生成章节元数据**
- 输入：章节内容列表
- 逻辑：计算每章的字符数、行数等信息
- 输出：完整的章节列表

**步骤7：保存结果到JSON文件**
- 输入：章节列表
- 逻辑：序列化为JSON并保存到文件系统
- 输出：JSON文件路径

---

## 三、功能需求

### 3.1 功能列表

| 编号 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| F-001 | 章节标题识别 | P0 | 基于正则表达式识别常见章节格式 |
| F-002 | 章节内容分割 | P0 | 根据标题位置分割章节内容 |
| F-003 | 章节数量验证 | P0 | 确保章节数不少于3个 |
| F-004 | 结果持久化 | P0 | 将分割结果保存为JSON文件 |
| F-005 | 误匹配过滤 | P1 | 过滤目录、注释中的误匹配 |
| F-006 | 自定义正则 | P1 | 支持用户自定义章节匹配规则 |
| F-007 | 章节预览 | P2 | 提供章节分割结果预览 |

### 3.2 输入输出

**输入：**
- novelId：小说ID（Long）
- cleanText：清洗后的文本内容（String，来自文本解析模块）

**输出：**
- ChapterSplitResult对象，包含：
  - novelId：小说ID
  - chapterCount：章节数量
  - chapters：章节列表
  - chapterFilePath：JSON文件路径
  - splitTime：分割耗时（毫秒）

### 3.3 支持的章节格式

| 格式类型 | 匹配模式 | 示例 |
|----------|----------|------|
| 中文章节 | `第[零一二三四五六七八九十百千万\d]+[章回节卷集部篇]` | 第一章、第1章、第十二回 |
| 数字章节 | `^\d+[\.\、]?\s*` | 1、1.、1、 |
| 英文章节 | `Chapter\s+\d+` | Chapter 1、Chapter One |
| 卷篇格式 | `[卷篇]\s*[零一二三四五六七八九十百千万\d]+` | 卷一、篇二 |
| 罗马数字 | `^[ⅠⅡⅢⅣⅤⅥⅦⅧⅨⅩⅪⅫ]+[\.\、]?\s*` | Ⅰ.、Ⅱ、 |
| 分隔符 | `^[=\-\*]{3,}$` | ===、---、*** |

### 3.4 业务规则

| 规则编号 | 规则描述 |
|----------|----------|
| BR-001 | 章节数量不得少于3个 |
| BR-002 | 章节标题不能为空 |
| BR-003 | 章节内容不能为空 |
| BR-004 | 章节顺序必须与原文一致 |
| BR-005 | 目录区域的章节标题应被过滤 |
| BR-006 | 章节分割结果必须保存到文件 |

---

## 四、接口设计

### 4.1 服务接口

ChapterSplitterService 提供三个核心方法：

- **splitChapters(novelId, cleanText)**：执行章节分割
  - 调用章节标题识别
  - 调用内容分割
  - 验证章节数量
  - 保存结果到文件
  - 返回 ChapterSplitResult

- **detectChapterTitles(text)**：识别章节标题
  - 使用正则表达式匹配
  - 过滤误匹配
  - 返回标题列表（包含位置信息）

- **loadChapters(filePath)**：从JSON文件加载章节
  - 读取JSON文件
  - 反序列化为章节列表
  - 返回 List<Chapter>

### 4.2 数据传输对象

#### ChapterSplitResult（分割结果）

| 字段 | 类型 | 说明 |
|------|------|------|
| novelId | Long | 小说ID |
| chapterCount | Integer | 章节数量 |
| chapters | List<Chapter> | 章节列表 |
| chapterFilePath | String | JSON文件路径 |
| splitTime | Long | 分割耗时（毫秒） |

#### Chapter（章节信息）

| 字段 | 类型 | 说明 |
|------|------|------|
| chapterNumber | Integer | 章节序号（从1开始） |
| title | String | 章节标题 |
| content | String | 章节内容 |
| charCount | Integer | 字符数 |
| lineCount | Integer | 行数 |
| startPosition | Long | 在原文中的起始位置 |
| endPosition | Long | 在原文中的结束位置 |

#### ChapterTitle（章节标题，用于识别阶段）

| 字段 | 类型 | 说明 |
|------|------|------|
| title | String | 标题文本 |
| position | Long | 在文本中的位置 |
| pattern | String | 匹配的模式名称 |

---

## 五、数据库设计

### 5.1 设计原则

1. **只存储文件路径**：章节内容保存为JSON文件，数据库只存路径
2. **与现有表结构兼容**：扩展现有 `novel_upload` 表，不创建新表
3. **减少数据库压力**：避免大文本字段影响查询性能

### 5.2 表结构变更

在 `novel_upload` 表中新增字段：

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| chapter_file_path | VARCHAR(500) | - | 章节JSON文件路径 |
| chapter_count | INT | DEFAULT 0 | 识别到的章节数（已有字段） |

### 5.3 实体类变更

NovelUpload 实体新增字段：

```java
@TableField("chapter_file_path")
private String chapterFilePath;
```

### 5.4 JSON文件格式

章节分割结果保存为JSON文件，格式如下：

```json
{
  "novelId": 1,
  "chapterCount": 5,
  "splitTime": "2024-01-15T10:30:00",
  "chapters": [
    {
      "chapterNumber": 1,
      "title": "第一章 初入江湖",
      "content": "章节正文内容...",
      "charCount": 15000,
      "lineCount": 350,
      "startPosition": 0,
      "endPosition": 15000
    },
    {
      "chapterNumber": 2,
      "title": "第二章 奇遇",
      "content": "章节正文内容...",
      "charCount": 12000,
      "lineCount": 280,
      "startPosition": 15000,
      "endPosition": 27000
    }
  ]
}
```

### 5.5 文件存储路径

```
uploads/
├── novels/                    # 小说文件
│   └── 2024/01/15/
│       └── xxx.txt
└── chapters/                  # 章节JSON文件
    └── novel_1_chapters.json
```

---

## 六、类设计

### 6.1 类结构

```
com.qiniu.novel2script
├── service/
│   ├── ChapterSplitterService           # 章节分割服务接口
│   └── impl/
│       └── ChapterSplitterServiceImpl   # 章节分割服务实现
├── splitter/
│   ├── ChapterDetector                  # 章节标题检测器
│   ├── ChapterPattern                   # 章节模式定义
│   └── ChapterFilter                    # 误匹配过滤器
├── dto/
│   ├── ChapterSplitResult               # 分割结果DTO
│   ├── Chapter                          # 章节DTO
│   └── ChapterTitle                     # 章节标题DTO
├── entity/
│   └── NovelUpload                      # 实体类（新增chapterFilePath字段）
├── mapper/
│   └── NovelUploadMapper                # Mapper接口
└── exception/
    └── ChapterSplitException            # 章节分割异常
```

### 6.2 核心类设计

#### ChapterSplitterService（服务接口）

```java
public interface ChapterSplitterService {
    /**
     * 执行章节分割
     * @param novelId 小说ID
     * @param cleanText 清洗后的文本
     * @return 分割结果
     */
    ChapterSplitResult splitChapters(Long novelId, String cleanText);
    
    /**
     * 识别章节标题
     * @param text 文本内容
     * @return 章节标题列表
     */
    List<ChapterTitle> detectChapterTitles(String text);
    
    /**
     * 从JSON文件加载章节
     * @param filePath JSON文件路径
     * @return 章节列表
     */
    List<Chapter> loadChapters(String filePath);
}
```

#### ChapterSplitterServiceImpl（服务实现）

依赖：
- ChapterDetector：章节标题检测
- ChapterFilter：误匹配过滤
- NovelUploadMapper：更新数据库
- StorageProperties：存储路径配置

**splitChapters 方法调用流程：**
1. 调用 detectChapterTitles 识别章节标题
2. 调用 ChapterFilter.filter 过滤误匹配
3. 调用 splitContent 分割章节内容
4. 验证章节数量是否≥3
5. 生成章节元数据（字符数、行数等）
6. 保存结果到JSON文件
7. 更新数据库中的 chapterFilePath 和 chapterCount
8. 返回 ChapterSplitResult

#### ChapterDetector（章节检测器）

负责使用正则表达式识别章节标题。

**核心属性：**
- patterns：预定义的正则表达式模式列表
- customPatterns：用户自定义的正则表达式

**核心方法：**
- **detect(text)**：检测文本中的章节标题
  - 遍历所有模式进行匹配
  - 收集匹配结果（标题、位置、模式）
  - 按位置排序返回

#### ChapterPattern（章节模式）

定义章节匹配的正则表达式模式。

**预定义模式：**
- CHINESE_CHAPTER：`第[零一二三四五六七八九十百千万\d]+[章回节卷集部篇]`
- NUMBER_CHAPTER：`^\d+[\.\、]?\s*\S+`
- ENGLISH_CHAPTER：`Chapter\s+\d+`
- VOLUME_CHAPTER：`[卷篇]\s*[零一二三四五六七八九十百千万\d]+`
- ROMAN_CHAPTER：`^[ⅠⅡⅢⅣⅤⅥⅦⅧⅨⅩⅪⅫ]+[\.\、]?\s*`
- SEPARATOR：`^[=\-\*]{3,}$`

#### ChapterFilter（过滤器）

负责过滤误匹配的章节标题。

**过滤规则：**
- 过滤目录区域的标题（通常在文本开头，连续出现多个标题）
- 过滤引文、注释中的标题
- 过滤过短的标题（少于2个字符）
- 过滤过长的标题（超过50个字符）

#### ChapterSplitException（异常类）

继承 RuntimeException，提供两个构造方法：
- message：异常消息
- message + cause：异常消息和原因链

### 6.3 核心类调用关系

```
ChapterSplitterServiceImpl
    │
    ├── ChapterDetector
    │   └── ChapterPattern（预定义模式）
    │
    ├── ChapterFilter
    │
    ├── NovelUploadMapper（更新数据库）
    │
    └── StorageProperties（获取存储路径）
```

---

## 七、算法设计

### 7.1 章节标题检测算法

```
输入：文本内容 text
输出：章节标题列表 titles

1. 初始化结果列表 titles = []

2. 遍历预定义的正则表达式模式 patterns
   a. 使用模式 pattern 编译正则表达式 regex
   b. 创建 Matcher 对 matcher = regex.matcher(text)
   c. 循环查找匹配：
      while (matcher.find()) {
          title = matcher.group()      // 匹配的标题文本
          position = matcher.start()   // 起始位置
          titles.add(new ChapterTitle(title, position, pattern.name()))
      }

3. 按 position 排序 titles

4. 去重：如果多个模式匹配到同一位置，保留第一个

返回 titles
```

### 7.2 误匹配过滤算法

```
输入：章节标题列表 titles，原始文本 text
输出：过滤后的标题列表 filteredTitles

1. 检测目录区域：
   - 如果前10个标题的间距都小于100字符，认为是目录区域
   - 移除目录区域的标题

2. 过滤无效标题：
   for each title in titles:
       if title.length < 2:  // 过短
           skip
       if title.length > 50:  // 过长
           skip
       if isInsideQuote(title, text):  // 在引号内
           skip
       filteredTitles.add(title)

3. 返回 filteredTitles
```

### 7.3 章节分割算法

```
输入：过滤后的标题列表 titles，原始文本 text
输出：章节列表 chapters

1. 初始化章节列表 chapters = []

2. 如果 titles 为空：
   - 将整个文本作为一个章节返回

3. 处理第一个章节（标题前的内容）：
   - 如果第一个标题位置 > 0：
     - 将标题前的内容作为"序章"或"前言"
     - 或者忽略（根据配置）

4. 分割章节：
   for i = 0 to titles.length - 1:
       start = titles[i].position
       end = (i < titles.length - 1) ? titles[i+1].position : text.length
       
       title = titles[i].title
       content = text.substring(start + title.length, end).trim()
       
       chapters.add(new Chapter(i+1, title, content))

5. 返回 chapters
```

### 7.4 目录检测算法

```
输入：标题列表 titles
输出：是否为目录区域

1. 如果 titles.size < 3:
   return false

2. 计算标题间距：
   for i = 1 to titles.size - 1:
       gap[i] = titles[i].position - titles[i-1].position

3. 计算平均间距：
   avgGap = sum(gap) / gap.length

4. 判断是否为目录：
   - 如果 avgGap < 100 且 所有间距都 < 200：
     return true（很可能是目录）

5. return false
```

### 7.5 文件保存算法

```
输入：ChapterSplitResult result，存储路径 basePath
输出：文件路径 filePath

1. 生成文件名：
   fileName = "novel_" + result.novelId + "_chapters.json"

2. 拼接完整路径：
   filePath = basePath + "/chapters/" + fileName

3. 创建目录：
   Files.createDirectories(parentPath)

4. 序列化为JSON：
   json = objectMapper.writeValueAsString(result)

5. 写入文件：
   Files.writeString(filePath, json, StandardCharsets.UTF_8)

6. 返回 filePath
```

---

## 八、异常处理

### 8.1 异常类定义

ChapterSplitException 继承 RuntimeException，提供两个构造方法：
- message：异常消息
- message + cause：异常消息和原因链

### 8.2 异常场景

| 异常场景 | 异常类型 | 处理方式 |
|----------|----------|----------|
| 文本为空 | ChapterSplitException | 抛出异常，提示文本为空 |
| 章节数少于3个 | ChapterSplitException | 抛出异常，提示章节数不足 |
| 章节标题为空 | ChapterSplitException | 抛出异常，提示标题为空 |
| 章节内容为空 | ChapterSplitException | 抛出异常，提示内容为空 |
| JSON文件写入失败 | IOException | 捕获并转换为ChapterSplitException |
| 数据库更新失败 | DataAccessException | 捕获并转换为ChapterSplitException |

### 8.3 全局异常处理

GlobalExceptionHandler 新增处理：
- ChapterSplitException -> 返回 400 状态码

---

## 九、配置项

### 9.1 chapter-splitter 配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| min-chapter-count | 3 | 最小章节数 |
| max-chapter-title-length | 50 | 最大标题长度 |
| min-chapter-title-length | 2 | 最小标题长度 |
| directory-detection-threshold | 100 | 目录检测阈值（字符间距） |
| custom-patterns | [] | 用户自定义正则表达式 |
| save-to-file | true | 是否保存到文件 |
| chapter-path | ${storage.base-path}/chapters | 章节文件存储路径 |

### 9.2 application.yml 配置示例

```yaml
chapter-splitter:
  min-chapter-count: 3
  max-chapter-title-length: 50
  min-chapter-title-length: 2
  directory-detection-threshold: 100
  custom-patterns: []
  save-to-file: true

storage:
  base-path: ./uploads
  chapter-path: ${storage.base-path}/chapters
```

---

## 十、测试用例

### 10.1 单元测试场景

| 测试场景 | 输入 | 预期结果 |
|----------|------|----------|
| 识别中文章节 | "第一章 开始\n内容...\n第二章 发展\n内容..." | 识别出2个章节 |
| 识别数字段落 | "1、开篇\n内容...\n2、发展\n内容..." | 识别出2个章节 |
| 识别英文章节 | "Chapter 1\nContent...\nChapter 2\nContent..." | 识别出2个章节 |
| 过滤目录区域 | 文本开头包含目录 | 目录被过滤 |
| 章节数不足 | 只有2个章节 | 抛出ChapterSplitException |
| 文本为空 | 空字符串 | 抛出ChapterSplitException |
| 保存JSON文件 | 正常章节列表 | 文件保存成功 |
| 从JSON加载 | 有效的JSON文件 | 加载成功 |

### 10.2 集成测试场景

| 测试场景 | 输入 | 预期结果 |
|----------|------|----------|
| 完整流程测试 | 包含5章的txt文件 | 分割成功，返回5个章节 |
| 大文件测试 | 10MB的txt文件 | 分割成功，性能在可接受范围 |
| 无章节文件 | 没有章节标记的文本 | 抛出异常 |
| 格式混乱文件 | 章节标记不规范 | 尽可能识别，不足3章则抛异常 |

### 10.3 测试数据

| 测试文件 | 类型 | 内容描述 | 预期结果 |
|----------|------|----------|----------|
| test-5chapters.txt | TXT | 包含5个标准中文章节 | 成功分割为5章 |
| test-english.md | MD | 包含3个英文章节 | 成功分割为3章 |
| test-no-chapter.txt | TXT | 无章节标记的小说 | 抛出异常 |
| test-with-toc.txt | TXT | 包含目录的小说 | 过滤目录，正确分割 |

---

## 十一、性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 小文件（<1MB）分割时间 | <200ms | 包含识别和分割 |
| 中等文件（1-5MB）分割时间 | <1000ms | 包含识别和分割 |
| 大文件（5-10MB）分割时间 | <3000ms | 包含识别和分割 |
| 内存占用 | <50MB | 处理最大文件时的内存峰值 |
| JSON文件写入时间 | <100ms | 保存分割结果 |

---

## 十二、后续优化

| 优化项 | 优先级 | 说明 |
|--------|--------|------|
| AI辅助识别 | P1 | 使用大模型辅助识别不规范的章节 |
| 手动调整 | P1 | 支持用户手动调整章节分割点 |
| 批量处理 | P2 | 支持多个文件并行分割 |
| 增量更新 | P2 | 支持只重新分割修改的章节 |
| 章节预览 | P2 | 提供章节分割结果预览界面 |
| 自定义规则 | P2 | 支持用户自定义章节匹配规则 |

---

**文档编写：** 张恒嘉
**最后更新：** 2026-06-06
**文档状态：** 待审核
